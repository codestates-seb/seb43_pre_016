server:
  address: localhost
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/preproject?createDatabaseIfNotExist=true&userUnicode=true&characterEncoding=UTF-8&characterSetResults=UTF-8&useSSL=true
    username: root
    password: 4759

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
mail:
  address:
    admin: oneyang@gmail.com
jwt:
  key: ${JWT_SECRET_KEY}               # 민감한 정보는 시스템 환경 변수설정
  access-token-expiration-minutes: 30
  refresh-token-expiration-minutes: 420
